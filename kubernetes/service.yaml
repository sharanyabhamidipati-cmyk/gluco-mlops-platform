apiVersion: v1
kind: Service
metadata:
  name: gluco-mlops-service
  labels:
    app: gluco-mlops-api
spec:
  type: NodePort
  selector:
    app: gluco-mlops-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
